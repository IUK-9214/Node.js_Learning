//create a function that will atuomatically export in the main app we just import the file 

const num1 =2;
const num2 =3;

function addvalue(){
    console.log(`the sum is ${num1+num2}`);
    }

addvalue();